/**
 * ActiveRules Site Library
 */ 

/**
 * Return a Site specfic configuration file.
 * 
 * @param object req
 * @returns object
 */
exports.getSiteConfig = function(hostname, configDir) {
  return bestHostConfig(hostname, configDir);
};

/**
 * Use the hostname for determining what Site to use for configuration.
 * A host config MUST have a site property, and its value must be a valid site alias.
 * Values in the host config
 * 
 * @param object req
 * @returns object
 */
function bestHostConfig(hostname, configDir) { 
console.log(hostname);
console.log(configDir);
    return hostname + ' ' + configDir;
  
}

